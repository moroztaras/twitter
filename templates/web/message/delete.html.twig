{% extends "Layout/layout-default.html.twig" %}
{% block main %}
    <div class="m-panel size-tb mg-c">
        <div class="m-content">
            <div class="x_panel">
                <div class="center">
                    <p class="bd-highlight">{{ 'delete_message_question'|trans }}?</p>
                    {{ include('form/delete.html.twig', { 'form' : form, 'cancel_url' : path('web_user_message_new', { uuid : uuid['uuid'] }) }) }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}