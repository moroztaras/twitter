<div class="center">
        <a href="{{ path('web_dialogue_followings_list') }}">
                <button type="submit" class="btn btn-primary">{{ 'new_dialogue'|trans|raw }}</button>
        </a>
</div>
{% for dialogue in dialogues %}
        {{ render(controller('App\\Controller\\Web\\DialogueController::dialogueUserInfo',{ 'dialogue' : dialogue.id })) }}
{% endfor %}