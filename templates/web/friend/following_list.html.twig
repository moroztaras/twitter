{% extends "Layout/layout-default.html.twig" %}

{% block title %}Following {% if user is not null %}| {{ user.firstName }} {{ user.lastName }}{% endif %}{% endblock %}
{% block main %}
    <div class="uk-section uk-section-xsmall">
        <div class="uk-container uk-container-xsmall">
            {% for following in followings %}
                <div class="uk-card uk-card-default uk-card-small uk-margin-small">
                    <div class="uk-card-header">
                        <div class="uk-grid-small uk-flex-middle" uk-grid>
                            <div class="uk-width-auto">
                                <img data-src="{% if following.friend.avatar != null %}/files/user/avatar/{{ following.friend.avatar }}{% else %}/images/system/no-profile-pic.png{% endif %}" width="40" height="40" alt uk-img>
                            </div>
                            <div class="uk-width-expand">
                                <div>
                                    <a class="uk-text-small uk-text-muted" href="{{ path('web_user_profile', {'id': following.friend.id}) }}">
                                        {{ following.friend.firstName }} {{ following.friend.LastName }}
                                    </a>
                                </div>
                                <p class="uk-text-meta uk-margin-remove-top">
                                    <time datetime="{{ following.friend.createdAt|date('Y-m-d H:i:s') }}">
                                        {{ following.friend.createdAt|date('M d, Y') }}
                                    </time>
                                </p>
                            </div>
                        </div>
                    </div>
                </div>
            {% endfor %}
        </div>
    </div>
{% endblock %}